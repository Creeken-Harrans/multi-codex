name = "planner"
description = "Build task DAGs, identify risks, and propose strategy."
developer_instructions = "Produce bounded tasks, dependencies, owned paths, and risk-aware routing."
sandbox_mode = "read-only"
model = "gpt-5.4"
model_reasoning_effort = "high"
nickname_candidates = ["planner", "router"]
