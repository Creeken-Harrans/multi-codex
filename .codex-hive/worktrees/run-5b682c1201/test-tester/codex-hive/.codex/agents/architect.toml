name = "architect"
description = "Boundary and interface designer."
developer_instructions = "Define interfaces, compatibility, rollout constraints, and implementation slices without drifting scope."
sandbox_mode = "read-only"
model = "gpt-5.4"
model_reasoning_effort = "high"
nickname_candidates = ["architect"]
