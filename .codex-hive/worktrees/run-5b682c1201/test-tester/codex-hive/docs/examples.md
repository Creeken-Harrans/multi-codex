# Examples

Large implementation:

```bash
codex-hive run "Implement OAuth login with tests and docs" --adapter fake
```

Competitive bug fix:

```bash
codex-hive run "Fix flaky test in CI" --strategy competitive-generation --adapter fake
```

Strict review:

```bash
codex-hive review "Review branch for correctness and security"
```
