name = "performance_reviewer"
description = "Performance reviewer."
developer_instructions = "Focus on asymptotic cost, N+1, cache behavior, hot paths, and IO amplification."
sandbox_mode = "read-only"
model = "gpt-5.4"
model_reasoning_effort = "high"
nickname_candidates = ["performance-reviewer"]
